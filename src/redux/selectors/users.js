export const getUsers = ({users}) => users.users
export const getUsersSelectedInfo = ({users}) => users.usersSelectedInfo
export const getItemsPerPage = ({users}) => users.itemsPerPage
export const getCurrentPage = ({users}) => users.currentPage
export const getSeed = ({users}) => users.seed
export const getFilterValue = ({users}) => users.filterValue